{
  "server": true,
  "node_name": "{{ node.name }}",
  "datacenter": "{{ datacenter }}",
  "data_dir": "{{ datadir }}",
  "bind_addr": "0.0.0.0",
  "client_addr": "0.0.0.0",
  "advertise_addr": "{{ node.addr }}",
  "bootstrap_expect": 3,
  "retry_join": ["{{ node1.addr }}", "{{ node2.addr }}", " {{ node3.addr }} "],
  "ui": true,
  "log_level": "DEBUG",
  "enable_syslog": true,
  "acl_enforce_version_8": false
}
